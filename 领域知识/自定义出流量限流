1. 消息队列保存所有请求
2. 每个key维护一个漏桶， 漏桶满时， 需要暂停消息的消费。（通过sleep， 消息丢弃等重试）
3. 消息暂停消费时， 消息队列会将请求丢到其他worker，其他worker也因为出流量限流无法消费，导致阻塞其他worker。
4. 所以不能让消息队列一直把消息往其他worker丢， 所以需要限制key所在worker的数量。 所以通过topickey数量限制，来完成。
5. 最好还是用多个消息队列
6. eb可以通过单worker限流 + 执行worker数量， 来整体限流
